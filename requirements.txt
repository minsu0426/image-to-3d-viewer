# ============================================================================
# 🚨 [설치 주의사항: 필수 코어 엔진 및 빌드 환경 세팅] 🚨
# 본 프로젝트는 3D 렌더링 엔진(C++ 컴파일)과 InstantMesh 코어 엔진이 필수입니다.
# `pip install` 실행 전, 시스템에 반드시 아래 환경이 세팅되어 있어야 합니다:
# 
# [필수 1] NVIDIA CUDA Toolkit 12.1 설치 (Visual Studio Integration 체크 해제 권장)
# [필수 2] Visual Studio 2022 C++ 빌드 도구 (파워쉘 대신 'x64 Native Tools' 창 사용)
#
# 👉 완전한 설치 순서:
# Step 1: git clone https://github.com/TencentARC/InstantMesh.git pipeline/instantmesh_core
# Step 2: python -m pip install --upgrade pip
# Step 3: pip install torch torchvision numpy wheel setuptools
# Step 4: pip install -r requirements.txt --no-build-isolation
# ============================================================================

# --- Base UI & Utils ---
streamlit
opencv-python
numpy
Pillow
psutil

# --- AI Core & PyTorch ---
torch
torchvision
einops
omegaconf

# --- 3D Processing ---
trimesh
open3d
huggingface_hub
plyfile
utils3d
git+https://github.com/NVlabs/nvdiffrast.git

# --- TRELLIS Dependencies ---
imageio
imageio-ffmpeg
rembg
xformers
spconv-cu121
ninja
easydict
scipy
tqdm
transformers
accelerate
safetensors
timm
kornia
onnxruntime

# --- SAM 2 Library ---
git+https://github.com/facebookresearch/sam2.git